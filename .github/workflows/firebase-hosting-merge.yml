# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: .env
    steps:
      - uses: actions/checkout@v4
      - name: Install and build
        env:
          VITE_LOCATIONIQ_API: ${{ secrets.VITE_LOCATIONIQ_API }}
          VITE_JAWGIO_API: ${{ secrets.VITE_JAWGIO_API }}
          VITE_FIREBASE_API: ${{ secrets.VITE_FIREBASE_API }}
          VITE_FIREBASE_URL: ${{ secrets.VITE_FIREBASE_URL }}
          VITE_OPENAI_API: ${{ secrets.VITE_OPENAI_API }}
          VITE_OPENAI_API_BACKUP: ${{ secrets.VITE_OPENAI_API_BACKUP }}
        run: | 
          npm ci 
          npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LABABLE_947E1 }}
          channelId: live
          projectId: labable-947e1
